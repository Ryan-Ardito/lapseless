/** Soft-deleted orgs can be restored within this window */
export const ORG_RECOVERY_WINDOW_DAYS = 30;
export const ORG_RECOVERY_WINDOW_MS = ORG_RECOVERY_WINDOW_DAYS * 24 * 60 * 60 * 1000;

/** Invite link validity */
export const INVITE_EXPIRY_MS = 7 * 24 * 60 * 60 * 1000;
